# replace requirements.txt
cat > requirements.txt <<'TXT'
streamlit==1.26.0
numpy==1.27.4
pandas==2.2.2
scikit-learn==1.3.2
matplotlib==3.8.0
pillow==10.1.0
rich==14.2.0
TXT

git add requirements.txt
git commit -m "Pin stable deps: avoid native builds"
git push
